name: baran_nuts
description: "A new Flutter project. version 1.0.0"
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.9.0
  go_router: ^16.1.0
  url_launcher: ^6.3.2
  url_strategy: ^0.3.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/main_image.webp
    - assets/avatar.png
